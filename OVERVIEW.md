# 项目概览 (Project Overview)

## 在线留言板系统 - 完整功能清单

### 📋 项目简介
这是一个功能完整的在线留言板系统，使用纯 Java Servlet + JDBC 实现，无需高级框架（如 Spring、Hibernate），支持图片上传、表情包插入、用户认证等功能，并具备完善的安全防护机制。

### ✨ 核心特性

#### 1. 用户功能
- ✅ **匿名发布**：未登录用户可以使用昵称发布留言，需完成数学验证码
- ✅ **用户登录**：支持用户名/密码登录
- ✅ **记住我**：可选择 30 天免登录（基于安全 Cookie + 数据库 Session）
- ✅ **自动登录**：Cookie 未过期时自动恢复登录状态
- ✅ **会话管理**：服务端 Session 管理，支持超时控制

#### 2. 留言功能
- ✅ **文本留言**：支持最长 1000 字符的留言内容
- ✅ **图片附件**：支持上传 PNG/JPEG/GIF 格式图片，最大 2MB
- ✅ **图片预览**：上传前实时预览图片
- ✅ **表情包**：28 种常用 Unicode 表情快速插入
- ✅ **留言列表**：按时间倒序显示所有留言
- ✅ **缩略图展示**：留言中的图片以缩略图形式展示
- ✅ **大图查看**：点击缩略图可查看原图
- ✅ **时间显示**：智能时间显示（刚刚、X分钟前、X天前）

#### 3. 管理功能
- ✅ **管理员后台**：独立的管理面板
- ✅ **系统统计**：显示总留言数、在线用户等统计信息
- ✅ **留言管理**：管理员可删除任何留言
- ✅ **级联删除**：删除留言时自动删除关联的图片文件
- ✅ **权限控制**：普通用户无法访问管理功能

#### 4. 安全特性

##### SQL 注入防护
- ✅ 所有数据库操作使用 `PreparedStatement`
- ✅ 参数化查询，不拼接 SQL 字符串
- ✅ 输入验证和类型检查

##### XSS 防护
- ✅ HTML 实体转义（`<`, `>`, `&`, `"`, `'`, `/`）
- ✅ Content-Security-Policy (CSP) 响应头
- ✅ 输出时强制转义所有用户输入内容

##### CSRF 防护
- ✅ Token 基础的 CSRF 防护
- ✅ 每个会话生成唯一 Token
- ✅ 所有状态变更操作（POST/PUT/DELETE）验证 Token
- ✅ Token 通过表单和 HTTP 头两种方式传递

##### 文件上传安全
- ✅ 文件类型白名单验证（仅允许 image/png, image/jpeg, image/gif）
- ✅ 文件大小限制（2MB）
- ✅ MIME 类型验证
- ✅ ImageIO 二次验证（确保文件内容为真实图片）
- ✅ UUID 随机文件名（防止文件名冲突和路径穿越）
- ✅ 路径规范化检查（防止 `../` 攻击）

##### 图片访问控制
- ✅ 专用 ImageServlet 统一输出图片
- ✅ 文件名安全验证（正则表达式白名单）
- ✅ 路径穿越检查
- ✅ Canonical Path 验证
- ✅ X-Content-Type-Options: nosniff 响应头
- ✅ 合理的缓存策略

##### 密码安全
- ✅ SHA-256 哈希存储
- ✅ 不明文存储密码
- ✅ 服务端验证

##### 会话安全
- ✅ HttpOnly Cookie（防止 JavaScript 访问）
- ✅ 会话超时机制（30 分钟）
- ✅ 安全的会话 ID 生成
- ✅ 登出时清除会话和 Cookie

### 🏗️ 技术架构

#### 后端技术栈
```
Java Servlet 4.0 (javax.servlet)
├── 模型层 (Model)
│   ├── Message.java - 留言实体
│   └── User.java - 用户实体
├── 数据访问层 (DAO)
│   ├── MessageDao.java - 留言数据访问
│   └── UserDao.java - 用户数据访问
├── Servlet 层 (Web)
│   ├── LoginServlet.java - 登录处理
│   ├── MessageServlet.java - 留言 CRUD + 图片上传
│   ├── ImageServlet.java - 图片安全输出
│   └── CaptchaServlet.java - 验证码生成
├── 过滤器 (Filter)
│   ├── AuthFilter.java - 认证过滤器
│   └── CsrfFilter.java - CSRF 防护
└── 工具类 (Util)
    ├── DBUtil.java - 数据库连接
    └── SecurityUtil.java - 安全工具（哈希、转义、Token生成）
```

#### 前端技术栈
```
JSP + JavaScript + CSS3
├── 视图层 (JSP)
│   ├── index.jsp - 主页（留言发表与展示）
│   ├── login.jsp - 登录页
│   ├── admin.jsp - 管理面板
│   └── error.jsp - 错误页面
├── 样式 (CSS)
│   └── styles.css - 响应式设计、卡片布局、渐变背景
└── 脚本 (JavaScript)
    └── app.js - AJAX、表单处理、图片预览、实时更新
```

#### 数据库设计
```sql
MySQL 8.0+
├── users 表 - 用户信息
│   ├── id (PK)
│   ├── username (UNIQUE)
│   ├── password (SHA-256)
│   ├── email
│   ├── is_admin
│   └── created_at
├── messages 表 - 留言数据
│   ├── id (PK)
│   ├── content
│   ├── author
│   ├── user_id (FK)
│   ├── image_path
│   ├── ip_address
│   └── created_at
└── sessions 表 - 持久会话
    ├── id (PK)
    ├── user_id (FK)
    ├── created_at
    └── expires_at
```

### 📁 项目结构
```
web/
├── sql/
│   ├── schema.sql - 初始化数据库脚本
│   └── alter_001_add_image_path.sql - 迁移脚本
├── src/main/
│   ├── java/com/messageboard/
│   │   ├── dao/ - 数据访问对象
│   │   ├── filter/ - 过滤器
│   │   ├── model/ - 实体模型
│   │   ├── util/ - 工具类
│   │   └── web/ - Servlet 控制器
│   └── webapp/
│       ├── WEB-INF/
│       │   └── web.xml - Web 应用配置
│       ├── css/
│       │   └── styles.css - 样式表
│       ├── js/
│       │   └── app.js - 前端脚本
│       ├── admin.jsp - 管理页面
│       ├── error.jsp - 错误页面
│       ├── index.jsp - 首页
│       └── login.jsp - 登录页面
├── pom.xml - Maven 配置
├── README.md - 项目说明
├── DEPLOYMENT.md - 部署指南
├── TESTING.md - 测试指南
└── OVERVIEW.md - 项目概览（本文档）
```

### 🎨 界面设计

#### 视觉风格
- **主题色**：紫色渐变 (#667eea → #764ba2)
- **卡片式设计**：白色卡片，圆角阴影
- **响应式布局**：适配桌面和移动设备
- **现代化 UI**：扁平化设计，流畅过渡动画

#### 页面布局

**首页 (index.jsp)**
```
┌─────────────────────────────────────┐
│  📝 在线留言板                       │
│  用户信息 / 登录按钮                 │
├─────────────────────────────────────┤
│  💬 发表留言                         │
│  ┌─────────────────────────────────┐│
│  │ 😊 快速插入表情                  ││
│  │ [😀][😃][😄][😁]...              ││
│  ├─────────────────────────────────┤│
│  │ 留言内容文本框                   ││
│  ├─────────────────────────────────┤│
│  │ 📷 上传图片 [选择文件] [预览]    ││
│  ├─────────────────────────────────┤│
│  │ 验证码: 5 + 3 = ? [答案]         ││
│  └─────────────────────────────────┘│
│  [📤 发表留言]                       │
├─────────────────────────────────────┤
│  📬 留言列表                         │
│  ┌─────────────────────────────────┐│
│  │ 👤 用户名          🕐 2小时前   ││
│  │ 留言内容文本...                  ││
│  │ [缩略图]                         ││
│  └─────────────────────────────────┘│
│  ┌─────────────────────────────────┐│
│  │ 👤 admin [ADMIN]   🕐 5分钟前   ││
│  │ 管理员的留言...                  ││
│  │                      [Delete]   ││
│  └─────────────────────────────────┘│
└─────────────────────────────────────┘
```

**登录页 (login.jsp)**
```
┌─────────────────────────────────────┐
│                                     │
│       ┌───────────────────┐         │
│       │  🔐 用户登录       │         │
│       ├───────────────────┤         │
│       │ 用户名: [______]  │         │
│       │ 密码:   [______]  │         │
│       │ ☑ 记住我（30天）   │         │
│       │ [    登录    ]    │         │
│       │ [  返回首页  ]    │         │
│       ├───────────────────┤         │
│       │ 测试账号：        │         │
│       │ admin/admin123    │         │
│       └───────────────────┘         │
│                                     │
└─────────────────────────────────────┘
```

**管理面板 (admin.jsp)**
```
┌─────────────────────────────────────┐
│  🛠️ 管理面板                         │
│  Administrator: admin   [退出登录]   │
├─────────────────────────────────────┤
│  📊 系统统计                         │
│  ┌────────┐ ┌────────┐ ┌────────┐  │
│  │总留言数│ │在线用户│ │系统状态│  │
│  │  128   │ │   1    │ │ 运行中 │  │
│  └────────┘ └────────┘ └────────┘  │
├─────────────────────────────────────┤
│  📬 所有留言管理                     │
│  (所有留言列表，带删除按钮)          │
└─────────────────────────────────────┘
```

### 🔧 配置说明

#### 数据库配置
```bash
# 环境变量方式（推荐）
export DB_URL="jdbc:mysql://localhost:3306/messageboard?..."
export DB_USER="root"
export DB_PASSWORD="your_password"
```

#### 上传目录配置
```xml
<!-- web.xml -->
<context-param>
    <param-name>uploadDir</param-name>
    <param-value>/var/tmp/board_uploads</param-value>
</context-param>
```

#### 会话配置
```xml
<!-- web.xml -->
<session-config>
    <session-timeout>30</session-timeout>
    <cookie-config>
        <http-only>true</http-only>
        <secure>false</secure>  <!-- 生产环境改为 true -->
    </cookie-config>
</session-config>
```

### 📊 性能指标

#### 预期性能
- **页面加载**：< 1 秒（本地）
- **图片上传**：< 2 秒（2MB 图片）
- **留言发表**：< 500 毫秒
- **并发处理**：支持 50+ 并发用户
- **数据库连接**：连接池复用

#### 资源消耗
- **内存占用**：~100-200 MB（Tomcat + 应用）
- **磁盘空间**：5 MB（应用） + N × 2MB（图片）
- **数据库**：< 10 MB（千级留言）

### 🚀 快速开始

#### 最小化部署步骤
```bash
# 1. 创建数据库
mysql -u root -p < sql/schema.sql

# 2. 配置环境变量
export DB_URL="jdbc:mysql://localhost:3306/messageboard?..."
export DB_USER="root"
export DB_PASSWORD="password"

# 3. 构建项目
mvn clean package

# 4. 部署到 Tomcat
cp target/messageboard.war $TOMCAT_HOME/webapps/

# 5. 启动 Tomcat
$TOMCAT_HOME/bin/startup.sh

# 6. 访问应用
# http://localhost:8080/messageboard/
```

### ✅ 功能清单（已实现）

#### 必需功能
- ✅ 匿名用户发表留言（需验证码）
- ✅ 注册用户发表留言（免验证码）
- ✅ 用户登录/登出
- ✅ 记住我功能（Cookie + Session）
- ✅ 留言列表展示
- ✅ 管理员删除留言

#### 图片功能
- ✅ 图片上传（PNG/JPEG/GIF）
- ✅ 文件大小限制（2MB）
- ✅ 文件类型验证（MIME + ImageIO）
- ✅ 图片预览
- ✅ 缩略图展示
- ✅ 安全的图片访问

#### 表情功能
- ✅ 28 种 Unicode 表情
- ✅ 点击插入到光标位置
- ✅ 留言中正确显示

#### 安全功能
- ✅ SQL 注入防护
- ✅ XSS 防护
- ✅ CSRF 防护
- ✅ 路径穿越防护
- ✅ 文件上传安全
- ✅ 会话管理
- ✅ 密码哈希

#### UI/UX
- ✅ 响应式设计
- ✅ 现代化界面
- ✅ 流畅动画
- ✅ 用户友好

### 📝 待优化项（可选）

#### 功能增强
- ⬜ 留言分页（当前全量加载）
- ⬜ 留言搜索
- ⬜ 用户注册功能
- ⬜ 用户头像
- ⬜ 留言点赞
- ⬜ 留言回复（嵌套评论）
- ⬜ 实时通知
- ⬜ 图片编辑（裁剪、滤镜）

#### 性能优化
- ⬜ 数据库连接池（如 HikariCP）
- ⬜ Redis 缓存
- ⬜ 图片 CDN
- ⬜ Gzip 压缩
- ⬜ 静态资源版本化

#### 监控运维
- ⬜ 日志系统（Log4j2）
- ⬜ 监控面板
- ⬜ 健康检查端点
- ⬜ 备份恢复脚本

### 🐛 已知限制

1. **无分页**：留言列表一次性加载所有数据（当前限制 100 条）
2. **简单验证码**：仅使用数学题验证码，可考虑图形验证码
3. **单服务器**：不支持集群部署（Session 不共享）
4. **图片存储**：使用本地文件系统，可考虑对象存储（如 OSS）
5. **邮件通知**：未实现邮件通知功能

### 📚 相关文档

- **README.md** - 项目介绍和快速开始
- **DEPLOYMENT.md** - 详细部署指南
- **TESTING.md** - 完整测试清单
- **sql/schema.sql** - 数据库表结构
- **sql/alter_001_add_image_path.sql** - 升级迁移脚本

### 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

**开发环境要求：**
- JDK 1.8+
- Maven 3.6+
- MySQL 8.0+
- Tomcat 9.0+

**代码规范：**
- 遵循 Java 标准命名规范
- 注释清晰完整
- 安全第一

### 📄 许可证

MIT License

### 👥 联系方式

如有问题或建议，请通过以下方式联系：
- GitHub Issues
- Email: [项目维护者邮箱]

---

**最后更新时间**: 2025-12-25
**版本**: 1.0.0
**状态**: ✅ 生产就绪
